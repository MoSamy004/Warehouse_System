# خطوات تشغيل الموقع على النت (MongoDB - مجاني، من غير تسجيل دخول)

الموقع دلوقتي: **مفيش تسجيل دخول** — أي حد يفتح اللينك يدخل على طول. البيانات متخزنة على **MongoDB Atlas** (قاعدة بيانات مجانية) وبتتزامن على أي جهاز (موبايل أو كمبيوتر) لأنها متخزنة أونلاين مش على الجهاز نفسه.

ملاحظة مهمة: بما إن مفيش تسجيل دخول، أي حد معاه اللينك يقدر يضيف/يعدل/يمسح بيانات. لو ده يقلقك، قولي وأرجعلك تسجيل الدخول تاني بس من غير Firebase.

---

## الجزء 1: عمل قاعدة بيانات مجانية (MongoDB Atlas)

1. روح https://www.mongodb.com/cloud/atlas/register واعمل حساب مجاني.
2. لما يطلب منك تعمل Cluster، اختار **M0 (Free)**.
3. من **Database Access** (في القائمة الجانبية): اعمل مستخدم جديد (Username + Password) — احفظهم هتحتاجهم بعدين.
4. من **Network Access**: دوس **Add IP Address** واختار **Allow Access from Anywhere** (0.0.0.0/0) — ده مهم عشان الموقع يقدر يوصل لقاعدة البيانات.
5. من **Database → Connect** اختار **Drivers**، وانسخ الـ **Connection String** اللي هيكون شكله كده:
   ```
   mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/?retryWrites=true&w=majority
   ```
6. استبدل `username` و `password` بالبيانات اللي عملتها في خطوة 3. احتفظ بالسطر ده، هتحتاجه بعدين (اسمه `MONGODB_URI`).

---

## الجزء 2: رفع الكود على GitHub

1. روح https://github.com واعمل حساب لو معندكش.
2. دوس **New repository**، اديله اسم زي `warehouse-system`، خليه **Public** أو **Private** زي ما تحب، ودوس **Create repository**.
3. في صفحة الريبو الجديدة، دوس **uploading an existing file**.
4. اسحب كل الملفات والمجلدات اللي بعتهالك (`index.html`, `app.js`, `package.json`, ومجلد `api` بالكامل) وارميهم هناك.
5. دوس **Commit changes**.

---

## الجزء 3: النشر على Vercel (بيدعم قاعدة البيانات + الموقع مع بعض)

1. روح https://vercel.com وسجل دخول بحساب GitHub بتاعك (زرار **Continue with GitHub**).
2. دوس **Add New → Project**.
3. هتلاقي الريبو `warehouse-system` اللي عملته، دوس **Import**.
4. قبل ما تدوس Deploy، افتح **Environment Variables** وضيف:
   - **Name:** `MONGODB_URI`
   - **Value:** الـ connection string اللي نسخته من الجزء 1
5. دوس **Deploy** واستنى دقيقة أو اتنين.
6. هتاخد لينك حقيقي شغال على النت زي `https://warehouse-system.vercel.app` — افتحه من أي موبايل أو كمبيوتر وهيشتغل.
7. لو عايز دومين خاص بيك بدل الـ vercel.app: من صفحة المشروع في Vercel → **Settings → Domains** → أضف الدومين بتاعك (لازم تكون اشتريته من موقع زي Namecheap الأول).

---

## بعد كده

- أي تعديل تطلبه، هبعتلك الملفات المحدثة، وترفعها تاني على GitHub (نفس مجلد الريبو) — Vercel بيحدث الموقع أوتوماتيك لما تعمل commit جديد.
- البيانات هتفضل موجودة في MongoDB حتى لو غيرت الموقع أو الاستضافة.
